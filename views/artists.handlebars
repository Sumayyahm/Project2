        <br><br><br>
        <div> 
        <h1 class="h1tag">    {{artistHeader.artistname.dataValues.artist_name}}          </h1>
        </div>
        <hr>

    <ul class="artwork-display" id= "horizontal list">
         {{#each artistArtwork.Artwork}}
         <li>
            <img class="image" src={{this.dataValues.artwork_image}} >
           <br><br>
          <a class="uk-button uk-button-default button-position" href="#modal-center" uk-toggle>Read More</a>
          <div id="modal-center" class="uk-flex-top" uk-modal>
          <div class="uk-modal-dialog uk-modal-body uk-margin-auto-vertical">
          <button class="uk-modal-close-default" type="button" uk-close></button>
          <div>
           <p>Name: {{this.dataValues.artwork_name}}</p>
           <p>Description : {{this.dataValues.artwork_descript}}</p>
          </div>
          </div>
          </div>
          <br><br>
          </li>
          {{/each}}
        

    </ul>

